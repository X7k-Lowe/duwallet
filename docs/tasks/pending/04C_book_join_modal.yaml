id: T-04C
title: Join Book Modal & Tests
description: |
  • B-01 画面における JoinByCodeModal を完成させる。
  • Supabase `book_users` テーブルに招待コード (inviteCode) で参加申請 (approved=false) を作成。
  • 招待コードが無効な場合はエラートーストを表示。
  • Modal から申請完了で succeeded トーストを表示しリストをリフレッシュ。
  • コンポーネント:
    - JoinByCodeModal (hooks 入れ替え)
    - JoinByCodeForm (内部 form)
  • テスト:
    - Unit: JoinByCodeForm validation.
    - E2E: JoinBookByCode シナリオ (未承認状態)。
dependsOn: [T-04B]
outputs:
  - apps/web/src/components/client/JoinByCodeModal.tsx
  - apps/web/src/components/client/JoinByCodeForm.tsx
  - tests/component/JoinByCodeForm.test.tsx
  - e2e/JoinBookByCode.spec.ts
status: pending
