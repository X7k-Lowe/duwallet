id: T-07C
title: RLS & User Tests
description: |
  • `database_schema.yaml` の RLS ポリシーを user management 関連で補完 (book_users approver, same household etc)。
  • Supabase 行レベルセキュリティのテストを Node スクリプトで作成。
  • GitHub Actions で自動実行。
dependsOn: [T-07B]
outputs:
  - supabase/migrations/* (rls)
  - tests/integration/rls_user.spec.ts
status: pending
